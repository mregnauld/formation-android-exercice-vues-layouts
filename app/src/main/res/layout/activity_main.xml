<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
	xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:app="http://schemas.android.com/apk/res-auto"
	xmlns:tools="http://schemas.android.com/tools"
	android:layout_width="match_parent"
	android:layout_height="match_parent"
	tools:context="com.formationandroid.vueslayout.MainActivity">

	<ScrollView
		android:id="@+id/scrollview_article"
		android:layout_width="0dp"
		android:layout_height="0dp"
		app:layout_constraintBottom_toTopOf="@+id/bouton_retour"
		app:layout_constraintHorizontal_bias="0.0"
		app:layout_constraintLeft_toLeftOf="parent"
		app:layout_constraintRight_toRightOf="parent"
		app:layout_constraintTop_toTopOf="parent"
		app:layout_constraintVertical_bias="0.0">

		<TextView
			android:id="@+id/article"
			android:layout_width="match_parent"
			android:layout_height="wrap_content"
			android:padding="@dimen/main_marge_article"
			android:text="@string/main_article"
			android:textColor="@color/couleurMainArticle"
			android:textSize="@dimen/main_taille_article"/>

	</ScrollView>

	<Button
		android:id="@+id/bouton_retour"
		android:layout_width="0dp"
		android:layout_height="@dimen/main_hauteur_bouton_retour"
		android:background="@color/couleurMainBoutonRetour"
		android:onClick="onClickBoutonRetour"
		android:text="@string/main_bouton_retour"
		android:textColor="@android:color/white"
		app:layout_constraintBottom_toBottomOf="parent"
		app:layout_constraintLeft_toLeftOf="parent"
		app:layout_constraintRight_toRightOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>
